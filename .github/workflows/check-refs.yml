
name: Check ref tags

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Print tags 
      run: |
        echo "$GITHUB_REF"
        echo ${{ github.event_name }}
