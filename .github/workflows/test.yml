

name: TEST

on: push
    
jobs:
  build:
    name: TEST
    runs-on: ubuntu-latest

    steps:
    - name: SED
      run: |
        sudo apt-get update
        sudo apt-get install sed
        echo "hola mundo" | sed "s/hola/mundo/"
      shell: bash

